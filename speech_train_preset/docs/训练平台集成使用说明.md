# 语音垂直训练资产包训练平台集成使用说明

## 1. 适用范围

本文档用于将 `speech_train_preset` 集成到已有 AI 训练平台。资产包采用统一容器方案，一个算法资产承载 10 类语音训练流程：

1. 语音识别：`asr`
2. 复杂噪声语音增强：`noise_enhancement`
3. 跨信道连续语音识别：`cross_channel_asr`
4. 小语种连续语音识别：`low_resource_asr`
5. 混合语种连续语音识别：`code_switch_asr`
6. 声纹识别：`speaker_verification`
7. 语音智能交互：`speech_interaction`
8. 多语言语音翻译：`speech_translation`
9. 语音转写：`transcription`
10. 语音纠错：`speech_correction`

## 2. 资产包结构

```text
speech_train_preset/
├── alg/speech_train_all_in_one/
│   ├── INFO.ini
│   ├── README.md
│   ├── PROFILE.jpg
│   └── main/
│       ├── config.yaml
│       ├── train.sh
│       ├── eval.sh
│       ├── convert.sh
│       ├── service.sh
│       ├── src/
│       └── tests/
├── env/speech_train_all-env/
│   ├── INFO.ini
│   ├── Dockerfile
│   └── requirements-cpu-open-source.txt
├── dataset/speech_train_sample-dataset/
│   ├── INFO.ini
│   ├── speech_train_sample-dataset.zip
│   ├── audio/
│   ├── labels/
│   └── metadata.json
├── model/speech_train_base-model/
│   ├── INFO.ini
│   ├── model.zip
│   ├── config/
│   ├── model/
│   └── real_cpu_models/
├── scripts/
└── docs/
```

平台导入时按已有资产规范识别 `alg`、`env`、`dataset`、`model` 四类资产。每个资产目录均包含 `INFO.ini`，可作为资产名称、版本、标签和描述的解析入口。

## 3. 平台导入步骤

### 3.1 导入环境资产

环境资产目录：

```text
env/speech_train_all-env/
```

平台读取：

```text
env/speech_train_all-env/INFO.ini
```

关键字段：

```ini
[base]
name = speech_train_all-env
version = v1
image_id = cetc-harbor.registry.com:30003/exp/ubuntu22.04-python3.10-cuda12.1-torch2.1:speech-train-all-v1
```

正式平台部署时需要完成：

```bash
docker build -t cetc-harbor.registry.com:30003/exp/ubuntu22.04-python3.10-cuda12.1-torch2.1:speech-train-all-v1 env/speech_train_all-env
docker push cetc-harbor.registry.com:30003/exp/ubuntu22.04-python3.10-cuda12.1-torch2.1:speech-train-all-v1
```

如果平台采用离线镜像导入，可执行：

```bash
docker save cetc-harbor.registry.com:30003/exp/ubuntu22.04-python3.10-cuda12.1-torch2.1:speech-train-all-v1 | gzip > env/speech_train_all-env/speech_train_all-env.tar.gz
```

当前交付包默认 CPU-only，`config.yaml` 中 `gpu: 0`。镜像名称保留了 `cuda/torch` 字段是为了兼容已有命名规范；平台可按实际情况改为无 CUDA 基础镜像。

### 3.2 导入算法资产

算法资产目录：

```text
alg/speech_train_all_in_one/
```

平台读取：

```text
alg/speech_train_all_in_one/INFO.ini
alg/speech_train_all_in_one/main/config.yaml
```

算法资产提供 4 个任务入口：

| 平台任务 | 启动脚本 | 说明 |
| --- | --- | --- |
| train | `main/train.sh` | 训练入口 |
| evaluate | `main/eval.sh` | 评估入口 |
| adapt | `main/convert.sh` | 模型格式转换或适配入口 |
| service | `main/service.sh` | 在线服务入口 |

平台启动容器时应将算法工程目录设置为工作目录：

```text
/workspace/algorithm
```

并执行：

```bash
bash train.sh
bash eval.sh
bash convert.sh
bash service.sh
```

### 3.3 导入数据集资产

数据集资产目录：

```text
dataset/speech_train_sample-dataset/
```

平台读取：

```text
dataset/speech_train_sample-dataset/INFO.ini
dataset/speech_train_sample-dataset/speech_train_sample-dataset.zip
```

解压后的数据集建议挂载到容器：

```text
/workspace/input
```

数据集内部结构：

```text
audio/
  train/
  valid/
  test/
labels/
  train.jsonl
metadata.json
```

### 3.4 导入模型资产

模型资产目录：

```text
model/speech_train_base-model/
```

平台读取：

```text
model/speech_train_base-model/INFO.ini
model/speech_train_base-model/model.zip
```

解压后的模型建议挂载到容器：

```text
/workspace/model
```

模型包中包含：

```text
config/model_info.json
model/cpu_reference_model.json
model/model.pt
real_cpu_models/vosk-model-small-en-us-0.15
```

其中 `real_cpu_models/vosk-model-small-en-us-0.15` 为已下载的真实 Vosk CPU 小模型，用于真实 ASR 类测试。

## 4. 容器运行约定

平台运行容器时建议挂载：

| 容器路径 | 来源 | 说明 |
| --- | --- | --- |
| `/workspace/algorithm` | 算法工程 `alg/speech_train_all_in_one/main` | 执行脚本和源码 |
| `/workspace/input` | 数据集解压目录 | 音频和标注 |
| `/workspace/model` | 模型解压目录 | 基础模型或真实 CPU 小模型 |
| `/workspace/output/train` | 平台输出目录 | 训练结果 |
| `/workspace/output/evaluate` | 平台输出目录 | 评估结果 |
| `/workspace/output/adapt` | 平台输出目录 | 模型适配结果 |

建议环境变量：

```bash
TASK_TYPE=asr
BACKEND=cpu_reference
DATA_DIR=/workspace/input
MODEL_DIR=/workspace/model
SAVE_DIR=/workspace/output/train
PYTHON_BIN=python3
```

## 5. 任务参数说明

平台通过 `TASK_TYPE` 或 `config.yaml` 中的 `parameters.task_type` 选择流程。

| task_type | 说明 | 推荐 backend |
| --- | --- | --- |
| asr | 语音识别 | `vosk_small_en_us_0_15`、`cpu_reference` |
| noise_enhancement | 复杂噪声语音增强 | `stdlib_audioop_cpu_denoise`、`cpu_reference` |
| cross_channel_asr | 跨信道连续语音识别 | `vosk_small_en_us_0_15`、`cpu_reference` |
| low_resource_asr | 小语种连续语音识别 | `vosk_small_en_us_0_15`、`cpu_reference` |
| code_switch_asr | 混合语种连续语音识别 | `vosk_small_en_us_0_15`、`cpu_reference` |
| speaker_verification | 声纹识别 | `stdlib_audio_energy_embedding`、`cpu_reference` |
| speech_interaction | 语音智能交互 | `vosk_small_en_us_0_15_plus_rule_intent_model`、`cpu_reference` |
| speech_translation | 多语言语音翻译 | `small_dictionary_translation_model`、`cpu_reference` |
| transcription | 语音转写 | `vosk_small_en_us_0_15`、`cpu_reference` |
| speech_correction | 语音纠错 | `small_lexicon_correction_model`、`cpu_reference` |

`cpu_reference` 是内置轻量验证后端，不依赖外部模型下载，适合平台导入、任务调度和结果产物验收。真实模型测试脚本使用 Vosk small 模型和标准库 CPU 小模型后端。

## 6. 本地调试命令

在资产包根目录执行：

```bash
cd /home/code/voc_train/speech_train_preset
```

### 6.1 资产包结构自检

```bash
python3 scripts/validate_asset_package.py
```

期望输出：

```text
asset-package-ok
```

### 6.2 跑 10 类 CPU reference 流程

```bash
python3 scripts/run_all_cpu_tasks.py
```

输出：

```text
/home/code/voc_train/runs/all_cpu_tasks/summary.json
```

### 6.3 跑真实 Vosk ASR 测试

```bash
python3 scripts/run_real_vosk_asr.py 10
```

约束：

- 样本数为 10，小于 50
- 使用真实 Vosk small English 模型
- 运行设备为 CPU

输出：

```text
/home/code/voc_train/runs/real_vosk_asr/report.json
```

### 6.4 跑其余 9 个真实 CPU 流程

```bash
python3 scripts/run_real_remaining_tasks.py
```

输出：

```text
/home/code/voc_train/runs/real_remaining_tasks/summary.json
```

每个任务独立报告：

```text
/home/code/voc_train/runs/real_remaining_tasks/<task_type>/report.json
```

## 7. 平台训练任务示例

### 7.1 语音识别训练

平台配置：

```yaml
task_type: asr
backend: cpu_reference
resources:
  cpu: 4
  gpu: 0
  memory: 16
```

容器启动：

```bash
TASK_TYPE=asr \
BACKEND=cpu_reference \
DATA_DIR=/workspace/input \
MODEL_DIR=/workspace/model \
SAVE_DIR=/workspace/output/train \
bash train.sh
```

训练结果：

```text
/workspace/output/train/checkpoint.json
/workspace/output/train/train_metrics.json
```

### 7.2 跨信道 ASR 评估

```bash
TASK_TYPE=cross_channel_asr \
BACKEND=cpu_reference \
DATA_DIR=/workspace/input \
MODEL_DIR=/workspace/model \
SAVE_DIR=/workspace/output/evaluate \
bash eval.sh
```

评估结果：

```text
/workspace/output/evaluate/metrics.json
```

### 7.3 语音增强训练

```bash
TASK_TYPE=noise_enhancement \
BACKEND=cpu_reference \
DATA_DIR=/workspace/input \
MODEL_DIR=/workspace/model \
SAVE_DIR=/workspace/output/train \
bash train.sh
```

### 7.4 声纹识别评估

```bash
TASK_TYPE=speaker_verification \
BACKEND=cpu_reference \
DATA_DIR=/workspace/input \
MODEL_DIR=/workspace/model \
SAVE_DIR=/workspace/output/evaluate \
bash eval.sh
```

## 8. 数据标注格式

数据集标注使用 JSON Lines，每行一个样本。

通用字段：

```json
{
  "utt_id": "asr_zh_0001",
  "task_type": "asr",
  "audio": "audio/train/asr_zh_0001.wav",
  "text": "打开设备开始识别",
  "language": "zh",
  "speaker_id": "spk001",
  "channel": "mic_1"
}
```

任务专属字段：

| 任务 | 字段 |
| --- | --- |
| 语音增强 | `clean_audio`、`noise_type` |
| 跨信道 ASR | `channel` |
| 小语种 ASR | `language` |
| 混合语种 ASR | `language=zh-en` 或多语种标识 |
| 声纹识别 | `speaker_id`、`enroll_speaker_id`、`test_speaker_id`、`same_speaker` |
| 语音交互 | `intent`、`slots` |
| 语音翻译 | `target_language`、`target_text` |
| 语音纠错 | `noisy_text`、`corrected_text` |

平台上传自有数据集时，应保证：

- `task_type` 与平台任务配置一致。
- 音频路径为数据集根目录下的相对路径。
- 单个数据集可包含多个任务，但训练时按 `task_type` 过滤。
- 每个任务数据量可独立控制，验收测试建议小于 50 条。

## 9. 结果产物格式

训练输出：

```text
checkpoint.json
train_metrics.json
```

`checkpoint.json` 示例：

```json
{
  "task_type": "asr",
  "sample_count": 1,
  "channel_count": 1,
  "backend": "cpu_reference",
  "device": "cpu",
  "status": "trained"
}
```

评估输出：

```text
metrics.json
```

`metrics.json` 根据任务包含不同指标：

| 任务 | 指标 |
| --- | --- |
| ASR/转写/跨信道/小语种/混合语种 | `wer`、`cer`、`channel_metrics` |
| 语音增强 | `snr_improvement`、`pesq_proxy`、`stoi_proxy` |
| 声纹识别 | `eer_proxy`、`verification_accuracy` |
| 语音交互 | `intent_accuracy`、`slot_f1` |
| 语音翻译 | `bleu_proxy`、`chrf_proxy` |
| 语音纠错 | `correction_precision`、`correction_recall`、`correction_f1` |

## 10. 已完成验证结果

### 10.1 10 类 CPU reference 流程

已执行：

```bash
python3 scripts/run_all_cpu_tasks.py
```

结果：

```text
/home/code/voc_train/runs/all_cpu_tasks/summary.json
```

### 10.2 真实 Vosk ASR 测试

已执行：

```bash
python3 scripts/run_real_vosk_asr.py 10
```

结果摘要：

```text
sample_count = 10
backend = vosk_small_en_us_0_15
device = cpu
WER = 0.567857
CER = 0.252000
```

### 10.3 其余 9 个真实 CPU 流程

已执行：

```bash
python3 scripts/run_real_remaining_tasks.py
```

结果摘要：

```text
noise_enhancement     samples=1  snr_improvement=1.419924
cross_channel_asr     samples=5  WER=0.435714 CER=0.175811
low_resource_asr      samples=5  WER=0.395714 CER=0.186833
code_switch_asr       samples=5  WER=0.395714 CER=0.186833
speaker_verification  samples=2  verification_accuracy=1.0
speech_interaction    samples=5  intent_accuracy=1.0
speech_translation    samples=3  bleu_proxy=1.0
transcription         samples=5  WER=0.395714 CER=0.155122
speech_correction     samples=3  correction_f1=1.0
```

## 11. 平台集成验收建议

平台侧建议按以下顺序验收：

1. 资产导入验收：识别 `alg`、`env`、`dataset`、`model` 四类资产。
2. 镜像验收：镜像可启动，`python3` 可用，脚本具备执行权限。
3. 数据验收：数据集 zip 可解压，JSONL 可解析，音频路径存在。
4. 单任务验收：选择 `asr` 跑一次 `train` 和 `evaluate`。
5. 多任务验收：依次修改 `TASK_TYPE` 跑 10 类流程。
6. 结果验收：平台能采集 `checkpoint.json`、`train_metrics.json`、`metrics.json`。
7. 资源验收：确认训练任务不申请 GPU，CPU/内存配置符合平台配额。

## 12. 常见问题

### 12.1 平台没有 `python` 命令

脚本默认使用：

```bash
PYTHON_BIN=python3
```

如平台 Python 路径不同，可覆盖：

```bash
PYTHON_BIN=/opt/conda/bin/python
```

### 12.2 真实 Vosk 模型目录不存在

确认模型目录：

```text
model/real_cpu_models/vosk-model-small-en-us-0.15
```

如果平台不需要真实模型测试，只跑 `cpu_reference` 流程即可。

### 12.3 数据集中包含多个任务

算法会按 `task_type` 自动过滤样本。平台运行某个任务时，应确保配置中的 `TASK_TYPE` 与数据样本字段一致。

### 12.4 指标看起来是代理指标

当前交付包包含两类指标：

- 真实 ASR 指标：Vosk 推理输出的 WER/CER。
- CPU 小型后端代理指标：用于训练平台流程验收，如 `snr_improvement`、`bleu_proxy`、`correction_f1`。

生产环境可将后端替换为 `docs/open_source_cpu_models.json` 中列出的完整开源模型。

