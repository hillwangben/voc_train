# 最终过程文档与运行产物整理说明

## 整理目标

本次整理保留最终交付和集成所需文档，删除中间尝试报告和临时运行目录，使交付包只保留最新、可复验、可集成的信息。

## 保留文档

| 文档 | 说明 |
| --- | --- |
| `交付物清单.md` | 资产包内容和交付策略 |
| `训练平台集成使用说明.md` | 已有训练平台集成使用说明 |
| `容器镜像构建说明.md` | 容器镜像构建与导出说明 |
| `数据集规范.md` | 数据集目录和 JSONL 标注规范 |
| `数据集更新-每类50条说明.md` | 当前 10 类任务每类 50 条数据说明 |
| `10个CPU流程独立测试说明.md` | 10 个 CPU 流程拆分和独立运行说明 |
| `10类模型算法离线测试能力检查报告.md` | 工程、代码、依赖、模型、脚本和指标检查结论 |
| `开源CPU小模型选型.md` | 开源 CPU 小模型选型说明 |
| `open_source_cpu_models.json` | 机器可读开源 CPU 后端清单 |
| `验收说明.md` | 平台验收建议 |

## 删除的中间文档

以下文档属于过程中间尝试或旧测试报告，已删除：

- `真实CPU小模型测试报告.md`
- `其余9个流程真实CPU测试报告.md`
- `开源CPU小模型替换执行报告.md`
- `国内源CPU-Torch安装与复验报告.md`

相关结论已合并到最终检查报告和集成使用说明中。

## runs 目录保留内容

当前仅保留两类最新运行产物：

```text
runs/all_cpu_tasks/
runs/cpu_flows/
```

说明：

- `runs/all_cpu_tasks/summary.json`：10 个 CPU 流程汇总报告。
- `runs/all_cpu_tasks/<task_type>/summary.json`：汇总执行时每个任务的报告。
- `runs/cpu_flows/<task_type>/summary.json`：每个独立流程单独执行的最新报告。

## 删除的 runs 中间目录

以下中间运行目录已删除：

- `runs/cross_channel_asr/`
- `runs/real_vosk/`
- `runs/real_vosk_asr/`
- `runs/real_remaining_tasks/`

## 当前数据规模

数据集已更新为 10 类任务，每类 50 条，共 500 条样本。

## 当前验证状态

已完成：

```bash
python3 speech_train_preset/scripts/validate_asset_package.py
python3 -m unittest discover -s speech_train_preset/alg/speech_train_all_in_one/main/tests -p 'test_*.py'
python3 speech_train_preset/scripts/run_all_cpu_tasks.py
```

10 个独立流程均已执行并保留最新报告。
